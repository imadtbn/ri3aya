## اختبار محلي بعد دمج site-tags.js

في الصفحة الرئيسية، ظهر محمّل `site-tags.js` مرة واحدة. مع بقاء `gtmId` و`ga4Id` و`clarityId` و`adsenseClient` على `xxxxxxxxx` لم تُنشأ أي سكريبتات تتبع خارجية، وبقيت وحدة الإعلان الواحدة مخفية (`hidden`) دون حجز مساحة. كان `dataLayer` غير منشأ، وهو السلوك المقصود إلى حين إدخال معرف GTM حقيقي.
